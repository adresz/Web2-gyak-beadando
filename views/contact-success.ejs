<!DOCTYPE html>
<html lang="hu">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Üzenet elküldve – Magyarország Atlasz</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <%- include('partials/head', { title: "Üzenetek – Magyarország Atlasz" }) %>
    <style>
        body {
            background: linear-gradient(135deg, #f6d365 0%, #fda085 100%);
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
        }
        .card {
            background: rgba(255, 255, 255, 0.95);
            border: none;
            border-radius: 1rem;
            padding: 2rem;
            text-align: center;
            box-shadow: 0 10px 25px rgba(0,0,0,0.15);
            opacity: 0;
            transform: translateY(30px);
            animation: fadeInUp 0.6s forwards;
        }
        h2 {
            font-weight: 700;
            color: #333;
        }
        p {
            color: #555;
            font-size: 1.1rem;
        }
        .btn-primary {
            border-radius: 50px;
            padding: 0.75rem 2rem;
            font-weight: 600;
            transition: all 0.3s ease;
        }
        .btn-primary:hover {
            transform: scale(1.05);
            box-shadow: 0 6px 15px rgba(0,0,0,0.2);
        }
        @keyframes fadeInUp {
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
        /* Konfetti effekt */
        .confetti {
            position: fixed;
            top: -10px;
            left: 50%;
            transform: translateX(-50%);
            font-size: 2rem;
            animation: confettiFall 1.5s ease-out forwards;
        }
        @keyframes confettiFall {
            0% { transform: translate(-50%, -50px) rotate(0deg); opacity: 1; }
            100% { transform: translate(-50%, 200px) rotate(360deg); opacity: 0; }
        }
    </style>
</head>
<body>

<div class="container py-5 flex-grow-1 d-flex flex-column justify-content-center">
    <%- include('partials/header') %>

    <div class="row justify-content-center">
        <div class="col-md-6">

            <div class="card">
                <div style="font-size: 4rem; padding-bottom: 20px;">✅</div>
                <h2 class="mb-4">Köszönjük, <%= nev %>!</h2>
                <p>Az üzeneted sikeresen mentésre került.</p>
                <a href="/" class="btn btn-primary mt-3">Vissza a főoldalra</a>
            </div>

        </div>
    </div>
</div>

<script>
    // Egyszerű konfetti / emoji effekt a card betöltésére
    document.addEventListener('DOMContentLoaded', () => {
        for(let i=0;i<15;i++){
            const confetti = document.createElement('div');
            confetti.textContent = "🎉";
            confetti.className = 'confetti';
            confetti.style.left = `${Math.random() * 100}%`;
            confetti.style.fontSize = `${Math.random() * 24 + 16}px`;
            confetti.style.animationDuration = `${Math.random() * 1 + 1.2}s`;
            document.body.appendChild(confetti);
            setTimeout(() => confetti.remove(), 2000);
        }
    });
</script>

</body>
</html>
